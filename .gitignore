# Python
backend/venv/
backend/__pycache__/
*.pyc

# Node
frontend/node_modules/
frontend/.next/

# Environments
.env
.DS_Store

# Data (Optional: we keep data/raw_cases, data/cleaned_cases to push the MVP data, but we might want to ignore the FAISS index if it's too large, but for 20 cases it's tiny).
# backend/data/faiss_index.bin
